<% @books.each do |book| %>

<%= book.id %>
<%= link_to book.name, admins_book_path(book.id) %>
<%= book.author %>
<%= book.price.to_s(:delimited) %>
<%= book.genre.name %>

<% if book.is_active == true %>
    販売中
  <% else %>
   販売停止中
  <% end %>
<% end %>